[project]
name = "backend-strawberry"
version = "1.0.0"
requires-python = ">=3.10"
dependencies = [
    "fastapi",
    "uvicorn",
    "python-multipart",
    "numpy",
    "pillow",
    "opencv-python-headless",
    "torch",
    "torchvision"
]